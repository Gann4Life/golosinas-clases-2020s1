import golosinas.*
describe "Bombón duro" {
	
	const bombon = new BombonDuro()	
	
	test "Peso" {
		assert.that(bombon.peso() == 15)
	}
	
	test "Dureza" {
		assert.that(bombon.dureza() == 3)
	}
	
	test "Mordisco" {
		bombon.mordisco()
		assert.that(bombon.peso() == 14)
	}
}